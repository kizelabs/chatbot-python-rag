# Database (used by docker-compose, no need to change for local dev)
DATABASE_URL=postgresql+psycopg://chatbot:chatbot123@localhost:5432/chatbot_rag

# All API keys (Gemini, NVIDIA NIM, Exa) are entered via the Settings dialog in the app.
# No API keys need to be set here.
